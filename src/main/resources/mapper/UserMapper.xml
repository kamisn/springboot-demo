<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.example.demo.entity.User">
        select
            id,
            username,
            password,
            nickname,
            role,
            status,
            create_time,
            update_time
        from `user`
        where username = #{username}
    </select>
    <insert id="insertUser"
            parameterType="com.example.demo.entity.User"
            useGeneratedKeys="true"
            keyProperty="id">

        insert into `user`(
            username,
            password,
            nickname,
            role,
            status
        )
        values (
                   #{username},
                   #{password},
                   #{nickname},
                   #{role},
                   #{status}
               )
    </insert>
    <select id="selectById" resultType="com.example.demo.entity.User">
        SELECT id, username, password, nickname, role, status, create_time, update_time
        FROM `user`
        WHERE id = #{id}
    </select>

</mapper>